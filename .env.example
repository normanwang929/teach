# Teach Platform — 配置说明

## 环境变量配置

复制 `.env.example` 为 `.env`，填入你的 API Key / Cookie，然后运行：

```bash
# Linux / macOS
source .env
python scripts/search_course_materials.py "Python"

# Windows PowerShell
Get-Content .env | ForEach-Object { $name, $value = $_ -split '=', 2; [Environment]::SetEnvironmentVariable($name, $value) }
python scripts/search_course_materials.py "Python"
```

---

## 各数据源配置说明

### ✅ 无需配置的源（免费，即开即用）

| 源 | 说明 |
|---|---|
| `arxiv` | arXiv API，无需 Key |
| `wikipedia` | MediaWiki API，无需 Key |
| `podcasts` | 播客 RSS，无需 Key |

---

### ⚠️ 需要配置的源

#### 1. Bilibili（中文视频）

**获取 Cookie 步骤：**
1. 打开 https://www.bilibili.com 并登录
2. 按 `F12` 打开开发者工具
3. 切换到 **Network** 标签
4. 刷新页面，点击任意请求
5. 在 **Request Headers** 中找到 `Cookie:` 这一行
6. 复制整个 Cookie 值（很长的一串）

**配置方式（任选一种）：**

```bash
# 方式一：环境变量
export BILIBILI_COOKIE="your_cookie_here"

# 方式二：.env 文件
echo BILIBILI_COOKIE="your_cookie_here" >> .env

# 方式三：直接传入
python scripts/search_course_materials.py "Python" --bilibili-cookie "your_cookie"
```

> ⚠️ Cookie 有效期约 30 天，过期后需重新获取。

---

#### 2. 知乎（中文文章）

**获取 Cookie 步骤** 同上（在 https://www.zhihu.com 登录后获取）

```bash
export ZHIHU_COOKIE="your_cookie_here"
```

---

#### 3. YouTube（英文视频，可选）

1. 访问 https://console.cloud.google.com/apis/credentials
2. 创建项目 → 启用 YouTube Data API v3
3. 创建 API Key（免费配额：10,000 次/天）

```bash
export YOUTUBE_API_KEY="your_api_key_here"
```

---

## 测试配置

```bash
# 测试 Bilibili
python -c "import sys; sys.path.insert(0, 'scripts'); from sources.bilibili import Source; import os; s = Source(api_key=os.environ.get('BILIBILI_COOKIE')); print(s.search('Python', 3))"

# 测试 Zhihu
python -c "import sys; sys.path.insert(0, 'scripts'); from sources.zhihu import Source; import os; s = Source(api_key=os.environ.get('ZHIHU_COOKIE')); print(s.search('Python', 3))"

# 测试全部（含需要配置的源）
python scripts/search_course_materials.py "Python 编程" --sources all
```
